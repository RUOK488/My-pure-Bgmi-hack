name: Build BGMI Hack

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Compile
      run: |
        clang -dynamiclib -arch arm64 -o MyHack.dylib my_pure_hack.c
        echo "✅ Compilation complete!"
        file MyHack.dylib
        ls -la MyHack.dylib
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MyHack
        path: MyHack.dylib
        if-no-files-found: error
